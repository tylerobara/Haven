apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "haven.fullname" . }}-test-connection"
  labels:
    {{- include "haven.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "haven.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
