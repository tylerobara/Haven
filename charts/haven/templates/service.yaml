apiVersion: v1
kind: Service
metadata:
  name: {{ include "haven.fullname" . }}
  labels:
    {{- include "haven.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "haven.selectorLabels" . | nindent 4 }}
